// *sanity test : something wrong with the tools, not the tests themselves

// *Sanity Test is useful to check whether our test plugin is properly installed
test('sanity test', () => {
  expect(true).toBe(true);
});
